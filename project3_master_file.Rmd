---
title: "Project3"
author: "Matthew Guella"
date: "3/30/2020"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

####Please Insert your username here as a test
```{r}
#guellato414
```

#### Loading in the Libraries
```{r Libraries}
library(kernlab)
library(ggplot2)
library(lattice)
library(neuralnet)
library(caret)
library(class)
library(gmodels) 
library(olsrr)
```

####Loading the data
```{r Data}
bet<-read.csv("data.csv") #This is the data up until each fight
preprocesseddata<-read.csv("preprocessed_data.csv") #This includes the fight details
```

####Exploring the data
```{r Examination}
str(bet)
summary(bet) 
head(bet)
#Winner will be the the y value, it is the 6th column in data


summary(bet$weight_class)


str(preprocesseddata)
summary(preprocesseddata)
#Winner will be the y value, it is the 1st column in the data

```

####Cleaning the Data
```{r Cleaning}

#Getting rid of factors to run models :)
bet$R_fighter<- NULL
bet$B_fighter<-NULL
bet$Referee<-NULL
bet$date<-NULL
bet$location<-NULL

#Step 1: Randomize the Data
set.seed(123) #To make sure that the models are consistent
bet_r<- bet[sample(nrow(bet)),]

bet_mm<-as.data.frame(model.matrix(~ . -1, data=bet_r))

normalize <- function(x) { 
  return((x - min(x)) / (max(x) - min(x)))
}

bet_n<-as.data.frame(lapply(bet_mm, normalize))

str(bet_mm)
summary(bet_mm)
str(bet_n)

lm<-lm(Winner~., data=bet_mm)





str(data)
```


